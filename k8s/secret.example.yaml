# Optional: override the generated secret before deploying.
# Edit k8s/kustomization.yaml secretGenerator, or create this file and add
# it to kustomization.yaml under resources (after removing secretGenerator).
apiVersion: v1
kind: Secret
metadata:
  name: workout-api-secret
  namespace: workout
type: Opaque
stringData:
  AUTH_SECRET_KEY: change-me-to-a-long-random-string
